<form action="{{ url_for("search") }}">
    <input name="q" type="search" {% if search_query %}value="{{ search_query | escape }}"{% endif %}/>
    <input type="submit" value="Cercare"/>
    <p class="hint"><a href="{{ url_for("search_help") }}">Aiuto</a>.</p>
</form>